# ktools-kotlin

`ktools-kotlin/` is the Kotlin workspace for the broader ktools ecosystem.

It is the root entrypoint for Kotlin implementations of the ktools libraries.

## Current Contents

This workspace currently contains:

- `kcli/`
- `ktrace/`

## Where To Go Next

Use the child repos for concrete API and implementation details:

- [kcli](kcli)
- [ktrace](ktrace)

Use the shared [`../kbuild`](../kbuild) tool for workspace builds.
