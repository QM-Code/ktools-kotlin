# Bootstrap Demo

Exists for CI and as the smallest compile/import usage reference for the Kotlin
`kcli` SDK.

This demo intentionally stays minimal:

- create a `Parser`
- parse an empty command line
- report success without depending on any extra demo SDKs
