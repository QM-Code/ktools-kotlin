# `kconfig` In `ktools-kotlin`

`ktools-kotlin/` does not currently include a checked-in `kconfig`
implementation.

## Current Status

- the active workspace components are `kcli/` and `ktrace/`
- `kconfig` is still planned rather than implemented in the Kotlin workspace
- the website reads that status from this source markdown
