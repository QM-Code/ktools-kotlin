# `ki18n` In `ktools-kotlin`

`ktools-kotlin/` does not currently include a checked-in `ki18n`
implementation.

## Current Status

- the active workspace components are `kcli/` and `ktrace/`
- `ki18n` is still planned rather than implemented in the Kotlin workspace
- future implementation docs should replace this status page in-place
